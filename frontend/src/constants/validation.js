/**
 * Validation Constants
 * 
 * Centralized validation rules and limits.
 * Use these instead of magic numbers in validation logic.
 */

// Authentication validation
export const PASSWORD_MIN_LENGTH = 6;

// Leaderboard configuration
export const LEADERBOARD_MAX_ITEMS = 10;

// Game card configuration
export const GAME_CARD_MIN_WIDTH = 300;
